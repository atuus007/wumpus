package hu.nye.progtech.models;

public class Wumpus extends FieldObject{


    public Wumpus(char shortCut, char column, int row, int userID, int matrixLength) {
        super(shortCut, column, row, userID, matrixLength);
    }
}
